{
    "name": "api-new",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "apps/api-new/src",
    "projectType": "application",
    "targets": {
        "build": {
            "executor": "@nx/webpack:webpack",
            "outputs": ["{options.outputPath}"],
            "defaultConfiguration": "production",
            "options": {
                "target": "node",
                "compiler": "tsc",
                "outputPath": "dist/apps/api-new",
                "main": "apps/api-new/src/main.ts",
                "tsConfig": "apps/api-new/tsconfig.app.json",
                "assets": ["apps/api-new/src/assets"],
                "isolatedConfig": true,
                "webpackConfig": "apps/api-new/webpack.config.js"
            },
            "configurations": {
                "development": {},
                "production": {}
            }
        },
        "serve": {
            "executor": "@nx/js:node",
            "defaultConfiguration": "development",
            "options": {
                "buildTarget": "api-new:build"
            },
            "configurations": {
                "development": {
                    "buildTarget": "api-new:build:development"
                },
                "production": {
                    "buildTarget": "api-new:build:production"
                }
            }
        },
        "lint": {
            "executor": "@nx/eslint:lint",
            "outputs": ["{options.outputFile}"],
            "options": {
                "lintFilePatterns": ["apps/api-new/**/*.ts"]
            }
        },
        "test": {
            "executor": "@nx/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "apps/api-new/jest.config.ts"
            }
        }
    },
    "tags": []
}
